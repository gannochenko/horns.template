+ если хелпер возвращает undefined или null, в шаблон подставлять пустую строку
2) нужен способ экранирования {{ и }}
- выполнение closure в объекте
+ реализация {{> nested}}
+ реализация logic-less if
6) функция эскейпа нужна для {{{}}}
- нужна функция проброса объекта (массива) из php в js (json_encode() ?)
- реализация each
9) обращение к полю родителя через ../field